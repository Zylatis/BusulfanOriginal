require(GenSA) #library for simualted annealing
require(foreach)
require(xlsx)#for exporting
require(iterators)
require(parallel)
require(doParallel)
require(ggplot2)
require(gridExtra)
require(gdata)
